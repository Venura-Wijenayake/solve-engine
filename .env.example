# Copy this file to .env and fill in real values. .env is git-ignored.

# GitHub personal access token used to read public issues (read-only scope is enough).
# GITHUB_TOKEN=

# SQLAlchemy-style database URL for the local ticket store.
# DATABASE_URL=
